---
import katex from "katex";
export interface Props {
    formula: string;
    className?: string;
}
const { formula, className } = Astro.props;
let html = katex.renderToString(formula, {output: "html"})

---
<div class={className ? className : "latex-inline"} set:html={html}/>

