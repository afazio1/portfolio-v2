---
export interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="italic block" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
